import type { ImageSourcePropType } from 'react-native';

/** Promo hero art — “Start your delivery” card (`assets/transport-icons-manifest.json` → `driverImage`). */
export const HOME_PROMO_ILLUSTRATION: ImageSourcePropType = require('../../../../assets/images/driverImage.png');
